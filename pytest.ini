[pytest]
# Examples and integration scripts require the full AI stack (Ollama/LangChain,
# Playwright) and are not part of the unit suite.
norecursedirs = examples
addopts = --ignore=test_examples.py
filterwarnings =
    ignore::DeprecationWarning
