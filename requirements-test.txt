# Dependencies needed to run the unit test suite ONLY.
# The heavy AI stack (langchain-ollama, chromadb, influxdb-client, streamlit,
# ollama) is intentionally excluded: modules that use it import it defensively,
# so the suite runs without it. This keeps CI fast and reliable.
pytest>=7.0.0
pytest-cov>=4.0.0
sqlalchemy>=2.0.0
rich>=13.0.0
tabulate>=0.9.0
fastapi>=0.104.0
httpx>=0.24.0
pydantic>=2.0.0
requests>=2.31.0
scikit-learn>=1.3.0
numpy>=1.24.0
python-dotenv>=1.0.0
playwright>=1.40.0
